<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moondust.va1mos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconva1mos_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VA1mos">
        <activity
            android:name=".Grammar.TiklananSatirActivity"
            android:exported="false" />
        <activity
            android:name=".Grammar.GrammarActivity"
            android:exported="false"
             />
        <activity
            android:name=".KelimePaketi.KelimeActivity"
            android:exported="false"
            android:noHistory="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>